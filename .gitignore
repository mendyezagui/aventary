node_modules
.next
out
.vercel
.env*
!.env.example
.DS_Store
*.log
# Build / runtime
.next/
out/
.vercel/
# Env (committed templates only)
.env*
!.env.example
# Editor
.DS_Store
*.log
*.tsbuildinfo

# Cloudflare adapter output — generated by the build, like .next above.
# Without this, `git add -A` sweeps ~157 generated files into a commit.
.open-next/
.wrangler/

# Generated by scripts/build-preview.mjs — a design harness, not a deliverable.
preview/

# Supabase CLI scratch — version cache it writes on any invocation.
supabase/.temp/

# Claude Code personal overrides. settings.json is the shared, committed one;
# this is the per-machine file, and it holds one-off permission grants that are
# nobody else's business and would be wrong to inherit.
.claude/settings.local.json
