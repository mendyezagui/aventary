node_modules
.next
out
.vercel
.env*
!.env.example
.DS_Store
*.log
# Build / runtime
.next/
out/
.vercel/
# Env (committed templates only)
.env*
!.env.example
# Editor
.DS_Store
*.log
*.tsbuildinfo

# Cloudflare adapter output — generated by the build, like .next above.
# Without this, `git add -A` sweeps ~157 generated files into a commit.
.open-next/
.wrangler/

# Claude Code: shared team settings are committed (.claude/settings.json),
# personal overrides are not.
.claude/settings.local.json
