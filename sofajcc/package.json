{
  "name": "sofajcc-site",
  "version": "1.0.0",
  "private": true,
  "description": "Static site for sofajcc.org (SoFa Jewish Community Center) — hosted on Cloudflare Pages.",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.js -i tw-input.css -o assets/tailwind.css --minify",
    "watch:css": "tailwindcss -c tailwind.config.js -i tw-input.css -o assets/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "3.4.13",
    "@tailwindcss/forms": "0.5.9"
  }
}
