// OpenNext config for Cloudflare Workers (replaces @cloudflare/next-on-pages).
// See https://opennext.js.org/cloudflare for details.
import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({});
